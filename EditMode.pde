void EditModeClick() {
  // if left clicked
    if (mouseButton == LEFT) {
        // check if in range of selecting a vertex and if so select the vertex
        oneVertexSelect();
        edgeSelect();
    // if right clicked
    } else {
        
    }
}
